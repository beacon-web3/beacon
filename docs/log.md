# Update Log

## 2026-07-17

* **Creation**: Initialized OKF v0.1 bundle structure for Beacon docs.
* **Update**: Added YAML frontmatter (`type`, `title`, `description`, `tags`, `timestamp`) to 15 concept-heavy documents across product/, architecture/, tokenomics/, and api/.
* **Update**: Created root `index.md` with progressive-disclosure navigation for all OKF-annotated concepts.
* **Update**: Created subdirectory `index.md` files for product/, architecture/, tokenomics/, and api/.
* **Update**: Added YAML frontmatter (`title`, `description`, `timestamp`) to root `index.md` and 4 subdirectory `index.md` files. Moved `okf_version` into root frontmatter. Log file kept as plain markdown per OKF spec.
* **Update**: Removed subdirectory `index.md` files (product/, architecture/, tokenomics/, api/) — root index provides sufficient navigation at current scale.
* **Update**: Trimmed root `index.md` frontmatter to only `okf_version: "0.1"`.
* **Update**: Added `docs/index.md` reference to `AGENTS.md` Canonical Product Specs section.
